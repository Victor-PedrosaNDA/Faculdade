const prompt = require("prompt-sync")();

        let vetor=[];
        let n=2;
        let i,valor;
        for(i=0;i<=n;i++){

            valor=parseFloat(prompt("Digite os dados do vetor: "));
            vetor.push(valor);
        }

        for(i=0;i<=0;i++){

                console.log("O vetor eh: ",i,)
        }
